# C-014 — LoginPage

**Tipo:** tela  
**Depende de:** C-001 ✅, C-004 ✅ (Button), C-005 ✅ (Input), C-010 ✅ (Spinner)  
**Rota:** `/login`  

---

## Prompt

Cole este prompt no Claude Code:

---

Cria a tela de Login do Pense & Precifique.

**Localização:** `src/pages/auth/LoginPage.tsx`

**Visual de referência:** abrir `design/Pense e Precifique/Login.html` no browser — reproduzir fielmente.
**Screenshots de estados:** `design/Pense e Precifique/screenshots/` (usar como referência visual dos estados)

---

### Layout geral

Tela dividida em dois painéis lado a lado no desktop, painel único no mobile:

```
Desktop (> 760px):
┌──────────────────────┬──────────────────────┐
│   BrandPanel (teal)  │   Formulário         │
│   gradiente          │   de login           │
└──────────────────────┴──────────────────────┘

Mobile (≤ 760px):
┌──────────────────────┐
│  Logo + Wordmark     │  ← aparece só no mobile
│  Formulário          │
└──────────────────────┘
```

Container externo:
- `minHeight: 100vh`
- `display: grid`, `placeItems: center`
- `padding: 24px`
- `background: #FAFAF8`

Card principal (`login-card`):
- `width: min(960px, 100%)`
- `display: grid`, `gridTemplateColumns: 1fr 1fr`
- `background: #fff`
- `borderRadius: var(--r-card)` → `24px`
- `overflow: hidden`
- `boxShadow: 0 20px 60px -28px rgba(31,122,111,0.28), 0 2px 8px rgba(0,0,0,0.06)`
- `border: 1px solid #F0EEE9`
- `animation: fadeUp .5s ease both`

CSS a adicionar em `src/index.css`:
```css
.login-card { grid-template-columns: 1fr 1fr; }
.brand-col { display: flex; }
.mobile-logo { display: none; }

@media (max-width: 760px) {
  .login-card { grid-template-columns: 1fr !important; }
  .brand-col { display: none !important; }
  .mobile-logo { display: flex !important; }
}

@keyframes shake {
  0%, 100% { transform: translateX(0); }
  20%       { transform: translateX(-6px); }
  40%       { transform: translateX(6px); }
  60%       { transform: translateX(-4px); }
  80%       { transform: translateX(4px); }
}
```

---

### Painel esquerdo — BrandPanel

Gradiente teal com decoração:
- `background: linear-gradient(150deg, #2A9D8F 0%, #1F7A6F 78%, #15665C 100%)`
- `padding: 46px 44px`
- `position: relative`, `overflow: hidden`
- `display: flex`, `flexDirection: column`, `justifyContent: space-between`

Elementos decorativos (blobs orgânicos absolutos):
```tsx
// Blob 1 — topo direito
<div style={{
  position: 'absolute', width: 280, height: 280,
  borderRadius: '46% 54% 60% 40% / 50% 44% 56% 50%',
  background: 'rgba(255,255,255,0.10)',
  top: -90, right: -70,
  animation: 'floaty 9s ease-in-out infinite',
}} />
// Blob 2 — base esquerda
<div style={{
  position: 'absolute', width: 200, height: 200,
  borderRadius: '60% 40% 45% 55% / 55% 50% 50% 45%',
  background: 'rgba(249,115,22,0.30)',
  bottom: -60, left: -40,
  animation: 'floaty 11s ease-in-out infinite reverse',
}} />
```

Conteúdo do brand panel:
```tsx
// Topo: logo + wordmark
<div style={{ display: 'flex', alignItems: 'center', gap: 12, position: 'relative' }}>
  <div style={{
    width: 48, height: 48, borderRadius: 14,
    background: 'rgba(255,255,255,0.18)',
    display: 'grid', placeItems: 'center',
    backdropFilter: 'blur(4px)',
  }}>
    <Logo size={32} />
  </div>
  <Wordmark size={18} darkMode />
</div>

// Meio: headline
<div style={{ position: 'relative' }}>
  <h2 style={{
    margin: 0, fontSize: 28, fontWeight: 700,
    color: '#fff', lineHeight: 1.25, letterSpacing: '-0.02em',
  }}>
    Preço certo,<br />negócio saudável.
  </h2>
  <p style={{
    margin: '14px 0 0', fontSize: 15, color: 'rgba(255,255,255,0.82)', lineHeight: 1.6,
  }}>
    Calcule, orce e controle o estoque do seu ateliê em um só lugar.
  </p>
</div>

// Base: 3 bullets de benefícios
<div style={{ display: 'flex', flexDirection: 'column', gap: 11, position: 'relative' }}>
  {[
    'Precificação com margem real',
    'Orçamentos profissionais em PDF',
    'Controle de insumos e produtos',
  ].map((text, i) => (
    <div key={i} style={{ display: 'flex', alignItems: 'center', gap: 10 }}>
      <span style={{
        width: 20, height: 20, borderRadius: '50%',
        background: 'rgba(255,255,255,0.20)',
        display: 'grid', placeItems: 'center', flexShrink: 0,
      }}>
        <svg viewBox="0 0 24 24" width="11" height="11" fill="none"
          stroke="#fff" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round">
          <path d="m5 12.5 4.2 4.2L19 7"/>
        </svg>
      </span>
      <span style={{ fontSize: 13.5, color: 'rgba(255,255,255,0.88)', fontWeight: 500 }}>
        {text}
      </span>
    </div>
  ))}
</div>
```

---

### Painel direito — Formulário

`padding: 46px 46px 40px`, `display: flex`, `flexDirection: column`, `justifyContent: center`

**Logo mobile** (só aparece no mobile via CSS):
```tsx
<div className="mobile-logo" style={{ alignItems: 'center', gap: 10, marginBottom: 22 }}>
  <Logo size={34} />
  <Wordmark size={18} />
</div>
```

**Título:**
```tsx
<h1 style={{ margin: 0, fontSize: 26, fontWeight: 700, letterSpacing: '-0.02em', color: '#3A372F' }}>
  Bem-vinda de volta <span style={{ display: 'inline-block', animation: 'floaty 3s ease-in-out infinite' }}>👋</span>
</h1>
<p style={{ margin: '8px 0 26px', fontSize: 14.5, color: '#A29E96', lineHeight: 1.55 }}>
  Entre para continuar cuidando dos seus preços e orçamentos.
</p>
```

**Alerta de erro de autenticação** (aparece quando `authError === true`):
```tsx
{authError && (
  <div role="alert" style={{
    display: 'flex', gap: 10, alignItems: 'flex-start',
    background: '#FEF3F0', border: '1.5px solid #F6C6B7',
    color: '#C0492B', borderRadius: 10, padding: '12px 14px',
    marginBottom: 18, fontSize: 13.5, lineHeight: 1.45,
    animation: 'shake .45s ease',
  }}>
    <Icons.alertCircle style={{ color: '#D9603C', flexShrink: 0, marginTop: 1, width: 18, height: 18 }} />
    <span><strong style={{ fontWeight: 600 }}>E-mail ou senha incorretos.</strong> Tente novamente.</span>
  </div>
)}
```

**Formulário:**
```tsx
<form onSubmit={handleSubmit} noValidate style={{ display: 'flex', flexDirection: 'column', gap: 16 }}>
  <Input
    label="E-mail"
    type="email"
    placeholder="seuemail@email.com"
    value={email}
    onChange={setEmail}
    icon={<Icons.mail />}
    error={emailErr}
    autoComplete="email"
  />

  <Input
    label="Senha"
    type="password"
    placeholder="Sua senha"
    value={senha}
    onChange={setSenha}
    icon={<Icons.lock />}
    error={senhaErr}
    autoComplete="current-password"
  />

  {/* Link esqueci senha */}
  <div style={{ display: 'flex', justifyContent: 'flex-end', marginTop: -4 }}>
    <a href="#" style={{ fontSize: 13.5, fontWeight: 500, color: '#2A9D8F', textDecoration: 'none' }}
      onMouseOver={(e) => e.currentTarget.style.textDecoration = 'underline'}
      onMouseOut={(e) => e.currentTarget.style.textDecoration = 'none'}>
      Esqueci minha senha
    </a>
  </div>

  {/* Botão submit */}
  <Button
    variant="primary"
    type="submit"
    fullWidth
    disabled={loading}
    icon={loading ? <Spinner size={16} /> : undefined}
    iconRight={loading ? undefined : <span style={{ fontSize: 18, marginTop: -1 }}>→</span>}
    size="lg"
  >
    {loading ? 'Entrando…' : 'Entrar'}
  </Button>
</form>
```

**Divisor + link cadastro:**
```tsx
<div style={{ display: 'flex', alignItems: 'center', gap: 14, margin: '24px 0 18px' }}>
  <span style={{ flex: 1, height: 1, background: '#EFEDE8' }} />
  <span style={{ fontSize: 12.5, color: '#B7B4AD', fontWeight: 500 }}>ou</span>
  <span style={{ flex: 1, height: 1, background: '#EFEDE8' }} />
</div>

<p style={{ margin: 0, textAlign: 'center', fontSize: 14, color: '#A29E96' }}>
  Não tem conta?{' '}
  <a href="/cadastro" style={{ color: '#F97316', fontWeight: 600, textDecoration: 'none' }}
    onMouseOver={(e) => e.currentTarget.style.textDecoration = 'underline'}
    onMouseOut={(e) => e.currentTarget.style.textDecoration = 'none'}>
    Cadastre-se
  </a>
</p>
```

---

### Lógica de estado

```tsx
const [email, setEmail] = useState('')
const [senha, setSenha] = useState('')
const [touched, setTouched] = useState({ email: false, senha: false })
const [authError, setAuthError] = useState(false)
const [loading, setLoading] = useState(false)

const emailValido = /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email.trim())
const emailErr = touched.email && email.trim() === '' ? 'Informe seu e-mail.'
  : touched.email && !emailValido ? 'E-mail inválido.' : ''
const senhaErr = touched.senha && senha === '' ? 'Informe sua senha.' : ''

function handleSubmit(e: React.FormEvent) {
  e.preventDefault()
  setTouched({ email: true, senha: true })
  setAuthError(false)
  if (!emailValido || senha === '') return
  setLoading(true)
  // Mock: simula erro de auth após 950ms
  setTimeout(() => {
    setLoading(false)
    setAuthError(true)
  }, 950)
}
```

---

### Atualizar rota em `src/routes/index.tsx`

```tsx
import LoginPage from '../pages/auth/LoginPage'

// Substituir as rotas de login:
{ path: '/',      element: <LoginPage /> },
{ path: '/login', element: <LoginPage /> },
```

---

## Como testar

### Passo 1 — Subir o servidor
```bash
npm run dev
```
Abre `http://localhost:5174/login`

---

### Passo 2 — O que você deve ver (desktop)

```
┌──────────────────────┬──────────────────────────────┐
│  [logo] Pense&Preci  │  Bem-vinda de volta 👋        │
│                      │  Entre para continuar...      │
│  Preço certo,        │                               │
│  negócio saudável.   │  E-mail                       │
│                      │  ┌────────────────────────┐   │
│  Calcule, orce e...  │  │ ✉ seuemail@email.com   │   │
│                      │  └────────────────────────┘   │
│  ✓ Precificação...   │                               │
│  ✓ Orçamentos...     │  Senha                        │
│  ✓ Controle...       │  ┌────────────────────────┐   │
│                      │  │ 🔒 ••••••          👁  │   │
│  (fundo teal)        │  └────────────────────────┘   │
│                      │               Esqueci senha   │
│                      │  [ Entrar → ]                 │
│                      │  ─────── ou ───────           │
│                      │  Não tem conta? Cadastre-se   │
└──────────────────────┴──────────────────────────────┘
```

### Passo 3 — Testar validação

1. Clicar em "Entrar" sem preencher nada
2. Campos ficam com borda vermelha + mensagens de erro
3. Preencher e-mail inválido (ex: "teste") → "E-mail inválido."

### Passo 4 — Testar loading

1. Preencher e-mail válido + senha qualquer
2. Clicar "Entrar"
3. Botão muda para spinner + "Entrando…" durante ~1s
4. Aparece alerta vermelho "E-mail ou senha incorretos." com animação shake

### Passo 5 — Teste mobile (375px)

`F12` → 375px:
- Painel teal some
- Logo + Wordmark aparece no topo do formulário
- Layout em coluna única

### Passo 6 — Comparar com design

Abrir `design/Pense e Precifique/Login.html` lado a lado — visual deve ser idêntico.

---

## Checklist de validação

- [ ] Desktop: dois painéis lado a lado
- [ ] Painel esquerdo: gradiente teal + blobs animados + headline + bullets
- [ ] Painel direito: formulário com título, campos, link e botão
- [ ] Campo e-mail com ícone envelope
- [ ] Campo senha com toggle de mostrar/ocultar
- [ ] Validação: campos obrigatórios + e-mail inválido
- [ ] Loading: spinner no botão durante submit
- [ ] Alerta de erro: shake animation + mensagem vermelha
- [ ] Mobile: painel teal some, logo aparece no topo
- [ ] Link "Cadastre-se" leva para `/cadastro`
- [ ] Visual idêntico ao `Login.html`
- [ ] `npm run dev` sem erros TypeScript

## Commit

```bash
git add .
git commit -m "feat(auth): adiciona LoginPage com validação e estados de loading/erro"
```

## Aprendizados desta implementação

*(preencher após executar)*

- ✅
- ⚠️
- 🔜

## Ao concluir

Informe **"C-014 concluído"** para:
1. Atualizar status no `CONVERSION_BACKLOG.md`
2. Mover este arquivo para `claude/prompts/concluidos/`
3. Receber o **C-015** (CadastroPage)
