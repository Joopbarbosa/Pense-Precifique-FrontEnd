# C-012 — ActionMenu

**Tipo:** componente shared  
**Depende de:** C-001 ✅, C-007 ✅ (ModalShell)  
**Usado em:** Lista de Orçamentos, Lista de Insumos, Lista de Produtos, Lista de Clientes  

---

## Prompt

Cole este prompt no Claude Code:

---

Cria o componente `ActionMenu` do Pense & Precifique — o menu de 3 pontos que aparece em cada linha/card das listas.

**Localização:** `src/components/shared/ActionMenu.tsx`

**Visual de referência:** abrir `design/Pense e Precifique/Orcamentos.html` e clicar nos 3 pontos de qualquer linha. Ver também `design/Pense e Precifique/Produtos.html`.

---

### Adicionar ícones em `src/components/ui/Icons.tsx`

Adicionar ao objeto `Icons`:

```tsx
dots: (p?: React.SVGProps<SVGSVGElement>) => (
  <svg viewBox="0 0 24 24" width="18" height="18" fill="currentColor" {...p}>
    <circle cx="12" cy="5" r="1.6"/>
    <circle cx="12" cy="12" r="1.6"/>
    <circle cx="12" cy="19" r="1.6"/>
  </svg>
),
copy: (p?: React.SVGProps<SVGSVGElement>) => (
  <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" strokeWidth={1.7} strokeLinecap="round" strokeLinejoin="round" {...p}>
    <rect x="9" y="9" width="12" height="12" rx="2.5"/>
    <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/>
  </svg>
),
power: (p?: React.SVGProps<SVGSVGElement>) => (
  <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" strokeWidth={1.7} strokeLinecap="round" strokeLinejoin="round" {...p}>
    <path d="M12 3v6M6.3 6.3a8.5 8.5 0 1 0 11.4 0"/>
  </svg>
),
ban: (p?: React.SVGProps<SVGSVGElement>) => (
  <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" strokeWidth={1.7} strokeLinecap="round" {...p}>
    <circle cx="12" cy="12" r="8.5"/>
    <path d="M6.5 6.5l11 11"/>
  </svg>
),
```

---

### Interface

```tsx
export interface ActionMenuItem {
  label: string
  icon: React.ReactNode
  onClick: () => void
  danger?: boolean      // texto e hover vermelhos
  dividerBefore?: boolean  // linha separadora antes deste item
}

interface ActionMenuProps {
  items: ActionMenuItem[]
  align?: 'right' | 'left'  // alinhamento do dropdown (default: 'right')
}
```

---

### Estrutura visual

**Botão de trigger (3 pontos):**
- `width: 34px`, `height: 34px`, `borderRadius: 9px`
- `border: none`, `cursor: pointer`, `display: grid`, `placeItems: center`
- Fechado: `background: transparent`, `color: #8A8780`
- Aberto: `background: #F1F0EC`
- Hover: `background: #F1F0EC`

**Dropdown:**
- `position: absolute`, `top: 40px`
- `align === 'right'`: `right: 0` | `align === 'left'`: `left: 0`
- `zIndex: 40`
- `width: 180px`
- `background: #fff`
- `border: 1px solid #EFEDE8`
- `borderRadius: 12px`
- `boxShadow: 0 12px 30px -8px rgba(0,0,0,0.18)`
- `padding: 6px`
- `animation: pop .14s ease both`

**Item do menu:**
- `width: 100%`, `display: flex`, `alignItems: center`, `gap: 10px`
- `padding: 9px 10px`, `borderRadius: 8px`
- `border: none`, `background: transparent`
- `fontSize: 13.5px`, `fontWeight: 500`
- Normal: `color: #5C594F`, ícone `color: #A29E96`
- Danger: `color: #C0492B`, ícone `color: #C0492B`
- Hover normal: `background: #F7F5F1`
- Hover danger: `background: #FCF1ED`

**Divider:**
- `height: 1px`, `background: #EFEDE8`, `margin: 5px 8px`

---

### Comportamento

- Clicar no botão → abre/fecha o dropdown
- Clicar fora → fecha (usar `useRef` + `useEffect` com `mousedown`)
- Clicar em item → executa `onClick` do item e fecha o menu
- Tecla `Escape` → fecha

---

### Animação — adicionar em `src/index.css` se não existir

```css
@keyframes pop {
  from { opacity: 0; transform: scale(0.95) translateY(-4px); }
  to   { opacity: 1; transform: scale(1) translateY(0); }
}
```

---

### Implementação

```tsx
import React, { useState, useRef, useEffect } from 'react'
import { Icons } from '../ui/Icons'
import type { ActionMenuItem } from './ActionMenu'

export default function ActionMenu({ items, align = 'right' }: ActionMenuProps) {
  const [open, setOpen] = useState(false)
  const ref = useRef<HTMLDivElement>(null)

  useEffect(() => {
    const handler = (e: MouseEvent) => {
      if (ref.current && !ref.current.contains(e.target as Node)) {
        setOpen(false)
      }
    }
    document.addEventListener('mousedown', handler)
    return () => document.removeEventListener('mousedown', handler)
  }, [])

  useEffect(() => {
    if (!open) return
    const handler = (e: KeyboardEvent) => {
      if (e.key === 'Escape') setOpen(false)
    }
    document.addEventListener('keydown', handler)
    return () => document.removeEventListener('keydown', handler)
  }, [open])

  return (
    <div ref={ref} style={{ position: 'relative' }}>
      {/* Trigger */}
      <button
        onClick={(e) => { e.stopPropagation(); setOpen((o) => !o) }}
        aria-label="Mais ações"
        aria-expanded={open}
        style={{
          width: 34, height: 34, borderRadius: 9,
          border: 'none',
          background: open ? '#F1F0EC' : 'transparent',
          color: '#8A8780',
          cursor: 'pointer',
          display: 'grid',
          placeItems: 'center',
          transition: 'background .12s',
        }}
        onMouseEnter={(e) => { if (!open) e.currentTarget.style.background = '#F1F0EC' }}
        onMouseLeave={(e) => { if (!open) e.currentTarget.style.background = 'transparent' }}
      >
        <Icons.dots />
      </button>

      {/* Dropdown */}
      {open && (
        <div style={{
          position: 'absolute',
          top: 40,
          [align === 'right' ? 'right' : 'left']: 0,
          zIndex: 40,
          width: 180,
          background: '#fff',
          border: '1px solid #EFEDE8',
          borderRadius: 12,
          boxShadow: '0 12px 30px -8px rgba(0,0,0,0.18)',
          padding: 6,
          animation: 'pop .14s ease both',
        }}>
          {items.map((item, i) => (
            <React.Fragment key={i}>
              {item.dividerBefore && (
                <div style={{ height: 1, background: '#EFEDE8', margin: '5px 8px' }} />
              )}
              <button
                onClick={() => { item.onClick(); setOpen(false) }}
                style={{
                  width: '100%',
                  display: 'flex',
                  alignItems: 'center',
                  gap: 10,
                  padding: '9px 10px',
                  borderRadius: 8,
                  border: 'none',
                  background: 'transparent',
                  cursor: 'pointer',
                  textAlign: 'left',
                  fontFamily: 'inherit',
                  fontSize: 13.5,
                  fontWeight: 500,
                  color: item.danger ? '#C0492B' : '#5C594F',
                }}
                onMouseEnter={(e) => {
                  e.currentTarget.style.background = item.danger ? '#FCF1ED' : '#F7F5F1'
                }}
                onMouseLeave={(e) => {
                  e.currentTarget.style.background = 'transparent'
                }}
              >
                <span style={{ display: 'flex', color: item.danger ? '#C0492B' : '#A29E96' }}>
                  {item.icon}
                </span>
                {item.label}
              </button>
            </React.Fragment>
          ))}
        </div>
      )}
    </div>
  )
}
```

---

### Exportar

Atualizar `src/components/shared/index.ts`:
```ts
export { default as SectionTitle } from './SectionTitle'
export { default as ActionMenu } from './ActionMenu'
export type { ActionMenuItem } from './ActionMenu'
```

---

### Página de teste temporária

Atualizar o `DashboardPreview` em `src/routes/index.tsx`:

```tsx
import { SectionTitle, ActionMenu } from '../components/shared'
import type { ActionMenuItem } from '../components/shared'
import { Icons } from '../components/ui'

// Dentro do conteúdo do DashboardPreview:
const menuItemsOrcamento: ActionMenuItem[] = [
  { label: 'Ver detalhes', icon: <Icons.eye />, onClick: () => alert('Ver detalhes') },
  { label: 'Duplicar', icon: <Icons.copy />, onClick: () => alert('Duplicar') },
  { label: 'Cancelar', icon: <Icons.ban />, onClick: () => alert('Cancelar'), danger: true, dividerBefore: true },
]

const menuItemsProduto: ActionMenuItem[] = [
  { label: 'Ver detalhes', icon: <Icons.eye />, onClick: () => {} },
  { label: 'Editar', icon: <Icons.edit />, onClick: () => {} },
  { label: 'Duplicar', icon: <Icons.copy />, onClick: () => {} },
  { label: 'Desativar', icon: <Icons.power />, onClick: () => {}, danger: true, dividerBefore: true },
]

<div style={{ padding: 24 }}>
  <h2 style={{ color: '#3A372F', marginBottom: 24 }}>ActionMenu — Preview C-012</h2>

  <div style={{ display: 'flex', gap: 40, alignItems: 'flex-start', flexWrap: 'wrap' }}>

    {/* Simulação de linha de orçamento */}
    <div style={{ background: '#fff', border: '1px solid #EFEDE8', borderRadius: 12, padding: '14px 18px', display: 'flex', alignItems: 'center', gap: 16, minWidth: 320 }}>
      <span style={{ flex: 1, fontWeight: 600, color: '#3A372F' }}>#0042 — Mariana Costa</span>
      <ActionMenu items={menuItemsOrcamento} />
    </div>

    {/* Simulação de card de produto */}
    <div style={{ background: '#fff', border: '1px solid #EFEDE8', borderRadius: 12, padding: '14px 18px', display: 'flex', alignItems: 'center', gap: 16, minWidth: 280 }}>
      <span style={{ flex: 1, fontWeight: 600, color: '#3A372F' }}>Kit Convite Casamento</span>
      <ActionMenu items={menuItemsProduto} />
    </div>

  </div>
</div>
```

---

## Como testar

### Passo 1 — Subir o servidor
```bash
npm run dev
```
Abre `http://localhost:5174/dashboard`

---

### Passo 2 — O que você deve ver

```
ActionMenu — Preview C-012

┌─────────────────────────────────┬───┐
│ #0042 — Mariana Costa           │ ⋮ │
└─────────────────────────────────┴───┘

┌──────────────────────────┬───┐
│ Kit Convite Casamento    │ ⋮ │
└──────────────────────────┴───┘
```

### Passo 3 — Testar abertura

1. Clicar no ⋮ do orçamento
2. Dropdown aparece com animação
3. Itens: "Ver detalhes", "Duplicar", divider, "Cancelar" (vermelho)

### Passo 4 — Testar fechamento

- Clicar em item → executa ação (alert) e fecha
- Clicar fora do menu → fecha
- Pressionar Escape → fecha

### Passo 5 — Testar hover

- Item normal: fundo `#F7F5F1` no hover
- Item danger ("Cancelar"): fundo `#FCF1ED` rosado no hover

### Passo 6 — Comparar com design

Abrir `design/Pense e Precifique/Orcamentos.html` e acionar o menu de 3 pontos — visual deve ser idêntico.

### Passo 7 — Se algo não funcionar

**Menu não fecha ao clicar fora:**
Verificar se o `useEffect` com `mousedown` está registrando o listener corretamente.

**Animação não funciona:**
Verificar se `@keyframes pop` está em `src/index.css`.

**Erro de TypeScript:**
```bash
npx tsc --noEmit
```

---

## Checklist de validação

- [ ] Botão ⋮ com hover e estado aberto corretos
- [ ] Dropdown com animação `pop` ao abrir
- [ ] Itens normais: hover `#F7F5F1`
- [ ] Item danger: texto e ícone vermelhos, hover `#FCF1ED`
- [ ] Divider separando grupos de itens
- [ ] Fecha ao clicar fora
- [ ] Fecha ao clicar em item
- [ ] Fecha ao pressionar Escape
- [ ] Visual idêntico ao `Orcamentos.html`
- [ ] `npm run dev` sem erros TypeScript

## Commit

```bash
git add .
git commit -m "feat(shared): adiciona componente ActionMenu com dropdown e fechar automático"
```

## Aprendizados desta implementação

*(preencher após executar)*

- ✅
- ⚠️
- 🔜

## Ao concluir

Informe **"C-012 concluído"** para:
1. Atualizar status no `CONVERSION_BACKLOG.md`
2. Mover este arquivo para `claude/prompts/concluidos/`
3. Receber o **C-013** (AppLayout)
