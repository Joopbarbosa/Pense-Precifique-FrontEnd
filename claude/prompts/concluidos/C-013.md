# C-013 — AppLayout

**Tipo:** layout  
**Depende de:** C-002 ✅ (Sidebar), C-003 ✅ (TopBar)  
**Usado em:** todas as telas autenticadas — substitui o DashboardPreview temporário  

---

## Prompt

Cole este prompt no Claude Code:

---

Cria o componente `AppLayout` do Pense & Precifique — o layout principal que envolve todas as telas autenticadas.

**Localização:** `src/components/layout/AppLayout.tsx`

**Visual de referência:** abrir `design/Pense e Precifique/Dashboard.html` — ver a estrutura de sidebar + área de conteúdo.

---

### Props

```tsx
interface AppLayoutProps {
  active: 'dashboard' | 'clientes' | 'orcamentos' | 'insumos' | 'produtos' | 'producao' | 'config'
  children: React.ReactNode
}
```

---

### Estrutura

```
┌─────────────────────────────────────────────────┐
│ [Sidebar 220px fixo] │ [main-area flex:1]        │
│                      │ ┌─────────────────────┐   │
│                      │ │ TopBar (mobile only) │   │
│                      │ ├─────────────────────┤   │
│                      │ │ .content             │   │
│                      │ │ padding 34px 40px    │   │
│                      │ │ max-width 1280px     │   │
│                      │ │ {children}           │   │
│                      │ └─────────────────────┘   │
└─────────────────────────────────────────────────┘
```

---

### CSS — adicionar em `src/index.css`

```css
.app-shell {
  display: flex;
  min-height: 100vh;
}

.main-area {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
}

.content {
  padding: 34px 40px 56px;
  max-width: 1280px;
  width: 100%;
  margin: 0 auto;
}

@media (max-width: 767px) {
  .content {
    padding: 22px 18px 48px;
  }
}
```

---

### Implementação

```tsx
import { useState } from 'react'
import Sidebar from './Sidebar'
import TopBar from './TopBar'

interface AppLayoutProps {
  active: 'dashboard' | 'clientes' | 'orcamentos' | 'insumos' | 'produtos' | 'producao' | 'config'
  children: React.ReactNode
}

export default function AppLayout({ active, children }: AppLayoutProps) {
  const [drawerOpen, setDrawerOpen] = useState(false)

  return (
    <div className="app-shell">
      {/* Overlay mobile */}
      <div
        className={`scrim${drawerOpen ? ' show' : ''}`}
        onClick={() => setDrawerOpen(false)}
      />

      {/* Sidebar */}
      <Sidebar
        active={active}
        open={drawerOpen}
        onClose={() => setDrawerOpen(false)}
      />

      {/* Área principal */}
      <div className="main-area">
        <TopBar onMenuOpen={() => setDrawerOpen(true)} />
        <div className="content">
          {children}
        </div>
      </div>
    </div>
  )
}
```

---

### Atualizar `src/components/layout/index.ts`

```ts
export { default as Sidebar } from './Sidebar'
export { default as TopBar } from './TopBar'
export { default as AppLayout } from './AppLayout'
```

---

### Atualizar `src/routes/index.tsx`

Substituir o `DashboardPreview` temporário pelo `AppLayout` real em todas as rotas autenticadas. O arquivo de rotas deve ficar assim:

```tsx
import { createBrowserRouter } from 'react-router-dom'
import { Logo, Wordmark } from '../components/ui'
import AppLayout from '../components/layout/AppLayout'

// Placeholder genérico para telas ainda não implementadas
const Placeholder = ({ nome, active }: {
  nome: string
  active: 'dashboard' | 'clientes' | 'orcamentos' | 'insumos' | 'produtos' | 'producao' | 'config'
}) => (
  <AppLayout active={active}>
    <div style={{ display: 'flex', alignItems: 'center', gap: 10, marginBottom: 32 }}>
      <Logo size={36} />
      <Wordmark size={18} />
    </div>
    <h2 style={{ margin: 0, color: '#3A372F' }}>{nome}</h2>
    <p style={{ color: '#A29E96', marginTop: 8 }}>Tela em construção</p>
  </AppLayout>
)

// Placeholder para telas sem sidebar (auth)
const AuthPlaceholder = ({ nome }: { nome: string }) => (
  <div style={{ padding: 32, fontFamily: 'sans-serif', color: '#3A372F' }}>
    <div style={{ display: 'flex', alignItems: 'center', gap: 10, marginBottom: 24 }}>
      <Logo size={36} />
      <Wordmark size={18} />
    </div>
    <h2 style={{ margin: 0 }}>{nome}</h2>
    <p style={{ color: '#888' }}>Tela em construção</p>
  </div>
)

export const router = createBrowserRouter([
  // Auth (sem sidebar)
  { path: '/',           element: <AuthPlaceholder nome="Login" /> },
  { path: '/login',      element: <AuthPlaceholder nome="Login" /> },
  { path: '/cadastro',   element: <AuthPlaceholder nome="Cadastro" /> },
  { path: '/onboarding', element: <AuthPlaceholder nome="Onboarding" /> },

  // App (com sidebar)
  { path: '/dashboard',          element: <Placeholder nome="Dashboard" active="dashboard" /> },
  { path: '/clientes',           element: <Placeholder nome="Clientes" active="clientes" /> },
  { path: '/orcamentos',         element: <Placeholder nome="Lista de Orçamentos" active="orcamentos" /> },
  { path: '/orcamentos/novo',    element: <Placeholder nome="Criar Orçamento" active="orcamentos" /> },
  { path: '/orcamentos/:id',     element: <Placeholder nome="Detalhe do Orçamento" active="orcamentos" /> },
  { path: '/insumos',            element: <Placeholder nome="Lista de Insumos" active="insumos" /> },
  { path: '/insumos/novo',       element: <Placeholder nome="Cadastrar Insumo" active="insumos" /> },
  { path: '/insumos/:id',        element: <Placeholder nome="Detalhe do Insumo" active="insumos" /> },
  { path: '/produtos',           element: <Placeholder nome="Lista de Produtos" active="produtos" /> },
  { path: '/produtos/novo',      element: <Placeholder nome="Cadastrar Produto" active="produtos" /> },
  { path: '/produtos/:id/editar',element: <Placeholder nome="Editar Produto" active="produtos" /> },
  { path: '/produtos/:id',       element: <Placeholder nome="Detalhe do Produto" active="produtos" /> },
  { path: '/producao',           element: <Placeholder nome="Registro de Produção" active="producao" /> },
  { path: '/configuracoes',      element: <Placeholder nome="Configurações" active="config" /> },
])
```

---

## Como testar

### Passo 1 — Subir o servidor
```bash
npm run dev
```
Abre `http://localhost:5174/dashboard`

---

### Passo 2 — O que você deve ver (desktop)

```
┌─────────────────┬──────────────────────────────────┐
│ [logo] Pense&   │  [logo] Pense & Precifique        │
│        Precifiq │                                   │
│        Para art │  Dashboard                        │
│                 │  Tela em construção               │
│ ● Dashboard     │                                   │
│   Clientes      │                                   │
│   Orçamentos    │                                   │
│   ...           │                                   │
│                 │                                   │
│ → Sair          │                                   │
└─────────────────┴──────────────────────────────────┘
```

- Sidebar fixa à esquerda, conteúdo à direita
- Sem botão "Abrir menu" avulso no conteúdo
- Padding do conteúdo: `34px 40px`

### Passo 3 — Navegar entre rotas

Clicar em cada item da sidebar e verificar:
- [ ] `/dashboard` → "Dashboard" ativo na sidebar
- [ ] `/clientes` → "Clientes" ativo
- [ ] `/orcamentos` → "Orçamentos" ativo
- [ ] `/insumos` → "Insumos" ativo
- [ ] `/produtos` → "Produtos" ativo
- [ ] `/producao` → "Produção" ativo
- [ ] `/configuracoes` → "Configurações" ativo
- Sidebar permanece visível em todas as rotas

### Passo 4 — Teste mobile (375px)

`F12` → ícone celular → 375px:
- [ ] Sidebar some
- [ ] TopBar aparece no topo
- [ ] Hamburger abre drawer
- [ ] Conteúdo com padding reduzido `22px 18px`

### Passo 5 — Rotas de auth

Acessar `http://localhost:5174/login`:
- [ ] Sem sidebar — só logo + placeholder

### Passo 6 — Se algo não funcionar

**Sidebar sumindo ao navegar:**
Verificar se o `NavLink` no `Sidebar.tsx` ainda tem o `onClick` com verificação de `window.innerWidth < 768`.

**Conteúdo sem padding:**
Verificar se `.content` foi adicionado em `src/index.css`.

**Erro de TypeScript:**
```bash
npx tsc --noEmit
```

---

## Checklist de validação

- [ ] AppLayout com sidebar + conteúdo lado a lado no desktop
- [ ] Sidebar permanece visível ao navegar entre rotas
- [ ] Item ativo muda corretamente em cada rota
- [ ] Mobile: sidebar some, TopBar aparece, drawer funciona
- [ ] Padding do conteúdo: 34px 40px desktop, 22px 18px mobile
- [ ] Rotas de auth sem sidebar
- [ ] `npm run dev` sem erros TypeScript

## Commit

```bash
git add .
git commit -m "feat(layout): adiciona AppLayout e atualiza todas as rotas com sidebar"
```

## Aprendizados desta implementação

*(preencher após executar)*

- ✅
- ⚠️
- 🔜

## Ao concluir

Informe **"C-013 concluído"** para:
1. Atualizar status no `CONVERSION_BACKLOG.md`
2. Mover este arquivo para `claude/prompts/concluidos/`
3. Receber o **C-014** (LoginPage) — primeira tela real!
