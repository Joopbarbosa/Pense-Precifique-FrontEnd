# C-001 — Logo + Wordmark

**Tipo:** componente base  
**Status:** ✅ concluído — commit 52b3751  
**Depende de:** C-000 ✅  
**Usado em:** Sidebar, Login, Cadastro, Onboarding, todos os PDFs  

---

## Aprendizados desta implementação

- `logo.png` deve estar em `public/` — copiar do zip do design antes de rodar
- O componente começa com placeholder (quadrado teal "P&") e troca para `<img>` quando `public/logo.png` existir
- Versão `darkMode` usa branco + `#FFD9BF` para fundos escuros

---

## Prompt

Cole este prompt no Claude Code:

---

Cria dois componentes base do Pense & Precifique: `Logo` e `Wordmark`.

**Localização:** `src/components/ui/Brand.tsx`

### Componente Logo

```tsx
interface LogoProps {
  size?: number
}

function Logo({ size = 40 }: LogoProps) {
  return (
    <img
      src="/logo.png"
      width={size}
      height={size}
      alt="Pense & Precifique"
      style={{ display: 'block', objectFit: 'contain', transform: 'translateX(3%)' }}
    />
  )
}
```

### Componente Wordmark

```tsx
interface WordmarkProps {
  size?: number       // default: 17
  darkMode?: boolean  // default: false
}
```

**Versão padrão (darkMode: false):**
- "Pense" → `#2A9D8F`
- "&" → `#F97316`, margem `0 1px`
- "Precifique" → `#3A372F`

**Versão dark (darkMode: true):**
- "Pense" → `#ffffff`
- "&" → `#FFD9BF`
- "Precifique" → `#ffffff`

Estilo do container: `fontWeight 700`, `letterSpacing -0.01em`, `lineHeight 1.05`

### Exportar

```tsx
export { Logo, Wordmark }
```

Criar `src/components/ui/index.ts`:
```ts
export { Logo, Wordmark } from './Brand'
```

Atualizar `src/routes/index.tsx` para importar e exibir Logo + Wordmark nos placeholders:
```tsx
import { Logo, Wordmark } from '../components/ui'

const Placeholder = ({ nome }: { nome: string }) => (
  <div style={{ padding: 32, fontFamily: 'sans-serif' }}>
    <div style={{ display: 'flex', alignItems: 'center', gap: 10, marginBottom: 24 }}>
      <Logo size={36} />
      <Wordmark size={18} />
    </div>
    <h2 style={{ color: '#3A372F', margin: 0 }}>{nome}</h2>
    <p style={{ color: '#888' }}>Tela em construção</p>
  </div>
)
```

---

## Como testar

### Passo 1 — Copiar logo.png
Antes de rodar, copiar `logo.png` do zip do design para `public/logo.png`.

### Passo 2 — Subir o servidor
```bash
npm run dev
```
Abre `http://localhost:5174`

### Passo 3 — O que você deve ver
- Logo real no canto superior esquerdo
- "Pense" em teal, "&" em laranja, "Precifique" em escuro

### Passo 4 — Se a logo não aparecer
O arquivo `public/logo.png` não foi copiado. Verificar se existe na pasta `public/`.

---

## Checklist de validação

- [ ] `src/components/ui/Brand.tsx` criado
- [ ] `src/components/ui/index.ts` criado com export
- [ ] Logo real aparece (não placeholder)
- [ ] Wordmark com cores corretas
- [ ] Prop `darkMode` altera cores

## Commit
```bash
git add .
git commit -m "feat(ui): adiciona componentes Logo e Wordmark"
```
