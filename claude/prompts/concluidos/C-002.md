# C-002 — Sidebar

**Tipo:** componente base  
**Status:** ✅ concluído  
**Depende de:** C-001 ✅  

---

## Aprendizados desta implementação

- O ícone `gear` complexo (path longo) quebra no JSX — usar versão simplificada com 8 raios (`M12 2v2M12 20v2...`) ⚠️ será refinado no futuro
- O botão "Abrir menu" aparece no desktop até o C-003 (TopBar) e C-013 (AppLayout) ficarem prontos — comportamento esperado
- CSS das classes `.sidebar`, `.scrim` deve estar em `src/index.css` — sem isso o mobile não funciona
- `DashboardPreview` com `useState` necessário para testar o drawer antes do AppLayout existir
- O botão de fechar (X) dentro da sidebar só aparece no mobile via `.drawer-close { display: none }` no breakpoint desktop

## Pendências conhecidas

- [ ] Ícone gear será refinado para ficar igual ao design original (engrenagem com dentes)
- [ ] Botão "Abrir menu" será removido quando C-003 e C-013 estiverem prontos

