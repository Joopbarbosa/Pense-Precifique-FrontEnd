# C-015 — CadastroPage

**Tipo:** tela  
**Depende de:** C-001 ✅, C-004 ✅ (Button), C-005 ✅ (Input), C-010 ✅ (Spinner)  
**Rota:** `/cadastro`  

---

## Prompt

Cole este prompt no Claude Code:

---

Cria a tela de Cadastro do Pense & Precifique.

**Localização:** `src/pages/auth/CadastroPage.tsx`

**Visual de referência:** abrir `design/Pense e Precifique/Cadastro.html` no browser — reproduzir fielmente.

---

### Estrutura geral

Mesmo layout do Login — dois painéis lado a lado desktop, coluna única mobile. Reutilizar as classes CSS `.login-card`, `.brand-col`, `.mobile-logo` já existentes.

Container: mesmas regras do LoginPage.

Card: `width: min(1000px, 100%)`, `gridTemplateColumns: 1fr 1.15fr` (formulário ligeiramente maior).

BrandPanel: mesmo componente do Login — gradiente teal com blobs e bullets.

---

### Stepper (indicador de progresso)

```tsx
// Etapa 1 ativa, Etapa 2 inativa
function Stepper() {
  return (
    <div style={{ display: 'flex', alignItems: 'center', gap: 14, marginBottom: 24 }}>
      {/* Etapa 1 — ativa */}
      <div style={{ display: 'flex', alignItems: 'center', gap: 9 }}>
        <span style={{
          width: 28, height: 28, borderRadius: '50%',
          display: 'grid', placeItems: 'center',
          fontSize: 13, fontWeight: 700,
          background: '#2A9D8F', color: '#fff',
          boxShadow: '0 4px 10px -4px rgba(42,157,143,0.6)',
        }}>1</span>
        <span style={{ fontSize: 14, fontWeight: 600, color: '#3A372F', whiteSpace: 'nowrap' }}>
          Sua conta
        </span>
      </div>

      {/* Linha separadora */}
      <span style={{ flex: 1, height: 2, background: '#EFEDE8', borderRadius: 2, minWidth: 24 }} />

      {/* Etapa 2 — inativa */}
      <div style={{ display: 'flex', alignItems: 'center', gap: 9 }}>
        <span style={{
          width: 28, height: 28, borderRadius: '50%',
          display: 'grid', placeItems: 'center',
          fontSize: 13, fontWeight: 700,
          background: '#fff', color: '#B7B4AD',
          border: '1.5px solid #EFEDE8',
        }}>2</span>
        <span style={{ fontSize: 14, fontWeight: 500, color: '#A29E96', whiteSpace: 'nowrap' }}>
          Precificação
        </span>
      </div>
    </div>
  )
}
```

---

### Formulário — campos

Layout em grid two-col para campos de nome+empresa e senha+confirmar:

```css
/* Adicionar em src/index.css */
.two-col {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 14px;
}

@media (max-width: 600px) {
  .two-col { grid-template-columns: 1fr; }
}
```

**Campos (em ordem):**

1. **Grid two-col:** Nome + Nome da empresa
2. **E-mail** (largura total)
3. **Grid two-col:** Senha + Confirmar senha
4. **StrengthMeter** (abaixo do grid de senhas)
5. **LogoUpload** (upload opcional da logo)
6. **Botão submit**

---

### StrengthMeter (indicador de força da senha)

Só aparece quando `senha.length > 0`:

```tsx
function StrengthMeter({ senha }: { senha: string }) {
  if (!senha) return null

  // Score 0-4 baseado em critérios
  let score = 0
  if (senha.length >= 8) score++
  if (/[A-Z]/.test(senha)) score++
  if (/[0-9]/.test(senha)) score++
  if (/[^A-Za-z0-9]/.test(senha)) score++

  const levels = [
    { label: 'Muito fraca', color: '#E0613F' },
    { label: 'Fraca',       color: '#F2913C' },
    { label: 'Razoável',    color: '#E8B23A' },
    { label: 'Boa',         color: '#7FB84F' },
    { label: 'Forte',       color: '#2A9D8F' },
  ]
  const cur = levels[score]

  return (
    <div style={{ marginTop: 9, animation: 'fadeUp .2s ease both' }}>
      {/* 4 barras de progresso */}
      <div style={{ display: 'flex', gap: 5 }}>
        {[0, 1, 2, 3].map((i) => (
          <span key={i} style={{
            flex: 1, height: 5, borderRadius: 3,
            background: i < score ? cur.color : '#EFEDE8',
            transition: 'background .2s',
          }} />
        ))}
      </div>
      {/* Label */}
      <div style={{ display: 'flex', justifyContent: 'space-between', marginTop: 6, fontSize: 12 }}>
        <span style={{ color: '#A29E96' }}>
          {senha.length < 8 ? 'Use no mínimo 8 caracteres' : 'Boa combinação ajuda na segurança'}
        </span>
        <span style={{ fontWeight: 600, color: cur.color }}>{cur.label}</span>
      </div>
    </div>
  )
}
```

---

### LogoUpload (upload opcional da logo)

```tsx
function LogoUpload({ preview, onPick, onRemove }: {
  preview: string | null
  onPick: (src: string) => void
  onRemove: () => void
}) {
  const inputRef = useRef<HTMLInputElement>(null)

  const handleFile = (e: React.ChangeEvent<HTMLInputElement>) => {
    const file = e.target.files?.[0]
    if (!file) return
    const reader = new FileReader()
    reader.onload = () => onPick(reader.result as string)
    reader.readAsDataURL(file)
    e.target.value = ''
  }

  return (
    <div style={{
      display: 'flex', alignItems: 'center', gap: 16,
      border: preview ? '1.5px solid #EFEDE8' : '1.6px dashed #D4D0C8',
      background: preview ? '#fff' : '#FCFBF9',
      borderRadius: 14, padding: 14,
      transition: 'all .15s',
    }}>
      <input ref={inputRef} type="file" accept="image/*" style={{ display: 'none' }} onChange={handleFile} />

      {preview ? (
        <div style={{ position: 'relative', flexShrink: 0 }}>
          <img src={preview} alt="Logo" style={{
            width: 72, height: 72, borderRadius: '50%', objectFit: 'cover',
            border: '2px solid rgba(42,157,143,0.35)',
            animation: 'pop .25s ease both',
          }} />
          <button type="button" onClick={onRemove} aria-label="Remover logo" style={{
            position: 'absolute', top: -5, right: -5,
            width: 24, height: 24, borderRadius: '50%',
            background: '#fff', border: '1px solid #EFEDE8',
            boxShadow: '0 2px 6px rgba(0,0,0,0.12)',
            color: '#C0492B', display: 'grid', placeItems: 'center', cursor: 'pointer',
          }}>
            <Icons.x width={14} height={14} />
          </button>
        </div>
      ) : (
        <div style={{
          width: 52, height: 52, borderRadius: 14, flexShrink: 0,
          display: 'grid', placeItems: 'center',
          background: 'rgba(42,157,143,0.08)', color: '#2A9D8F',
        }}>
          <Icons.image />
        </div>
      )}

      <div style={{ flex: 1 }}>
        <div style={{ fontSize: 14, fontWeight: 600, color: '#3A372F', marginBottom: 3 }}>
          {preview ? 'Logo enviada' : 'Logo do ateliê'}
          <span style={{ fontSize: 12, fontWeight: 400, color: '#A29E96', marginLeft: 6 }}>opcional</span>
        </div>
        <div style={{ fontSize: 12.5, color: '#A29E96', marginBottom: preview ? 0 : 9 }}>
          {preview ? 'Aparece nos seus PDFs e orçamentos.' : 'PNG ou JPG, até 2 MB. Aparece nos PDFs.'}
        </div>
        {!preview && (
          <button type="button" onClick={() => inputRef.current?.click()} style={{
            height: 34, padding: '0 14px', borderRadius: 8,
            border: '1.5px solid #EFEDE8', background: '#fff',
            color: '#5C594F', fontSize: 13, fontWeight: 600,
            fontFamily: 'inherit', cursor: 'pointer',
          }}
            onMouseEnter={(e) => e.currentTarget.style.background = '#FAF8F5'}
            onMouseLeave={(e) => e.currentTarget.style.background = '#fff'}
          >
            Escolher arquivo
          </button>
        )}
      </div>
    </div>
  )
}
```

---

### Estado de sucesso

Quando `done === true`, substituir o formulário por:

```tsx
function SuccessNote({ nome }: { nome: string }) {
  return (
    <div style={{ textAlign: 'center', padding: '30px 0 10px', animation: 'fadeUp .4s ease both' }}>
      <div style={{
        width: 64, height: 64, borderRadius: '50%',
        margin: '0 auto', display: 'grid', placeItems: 'center',
        background: 'rgba(42,157,143,0.12)', color: '#2A9D8F',
        animation: 'pop .35s ease both',
      }}>
        <svg viewBox="0 0 24 24" width="30" height="30" fill="none">
          <path d="m4 12.5 5 5L20 6.5" stroke="#2A9D8F" strokeWidth="2.6" strokeLinecap="round" strokeLinejoin="round"/>
        </svg>
      </div>
      <h2 style={{ margin: '18px 0 6px', fontSize: 22, fontWeight: 700, color: '#3A372F' }}>
        Conta criada{nome ? `, ${nome.split(' ')[0]}` : ''}! 🎉
      </h2>
      <p style={{ margin: 0, fontSize: 14.5, color: '#A29E96', lineHeight: 1.55 }}>
        Agora vamos para a <strong style={{ color: '#2A9D8F' }}>Etapa 2 — Precificação</strong> para configurar seus custos.
      </p>
    </div>
  )
}
```

---

### Validações

```tsx
const emailValido = /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email.trim())
const erros = {
  nome:    touched.nome    && !nome.trim()           ? 'Como podemos te chamar?' : '',
  empresa: touched.empresa && !empresa.trim()        ? 'Informe o nome do seu ateliê.' : '',
  email:   emailEmUso      ? 'Este e-mail já está em uso. Faça login ou use outro e-mail.'
         : touched.email && !email.trim()            ? 'Informe seu e-mail.'
         : touched.email && !emailValido             ? 'E-mail inválido.' : '',
  senha:   touched.senha   && senha.length < 8       ? 'A senha precisa de no mínimo 8 caracteres.' : '',
  senha2:  touched.senha2  && senha2 !== senha        ? 'As senhas não coincidem.' : '',
}
```

E-mails já em uso (mock): `['ana@atelier.com', 'teste@teste.com']`

Submit:
- Marca todos os campos como touched
- Valida todos os erros
- Se e-mail na lista mock → `emailEmUso = true`
- Se válido → `loading = true` → setTimeout 1000ms → `done = true`

---

### Atualizar rota em `src/routes/index.tsx`

```tsx
import CadastroPage from '../pages/auth/CadastroPage'

{ path: '/cadastro', element: <CadastroPage /> },
```

---

## Como testar

### Passo 1 — Subir o servidor
```bash
npm run dev
```
Abre `http://localhost:5174/cadastro`

---

### Passo 2 — O que você deve ver

```
┌──────────────────┬────────────────────────────────────┐
│  BrandPanel teal │  [1]──────────[2]                  │
│                  │  Sua conta    Precificação          │
│                  │                                     │
│                  │  Vamos criar sua conta!             │
│                  │  Leva menos de 2 minutos...         │
│                  │                                     │
│                  │  [ Nome ]         [ Empresa ]       │
│                  │  [ E-mail                       ]   │
│                  │  [ Senha ]        [ Confirmar  ]    │
│                  │  [====] Razoável                    │
│                  │  [ 🖼 Logo do ateliê (opcional) ]   │
│                  │  [ Próximo → ]                      │
│                  │  Já tem conta? Faça login           │
└──────────────────┴────────────────────────────────────┘
```

### Passo 3 — Testar validação

1. Clicar "Próximo" sem preencher → todos os campos com erro
2. Digitar senha fraca → barras vermelhas no StrengthMeter
3. Senha forte (8+ chars, maiúscula, número, símbolo) → barras teal
4. Senhas diferentes → erro "As senhas não coincidem"
5. E-mail `ana@atelier.com` → erro "E-mail já está em uso"

### Passo 4 — Testar upload de logo

1. Clicar "Escolher arquivo" → selecionar imagem
2. Preview circular aparece com botão X
3. Clicar X → volta ao estado sem logo

### Passo 5 — Testar submit com sucesso

1. Preencher todos os campos válidos
2. Clicar "Próximo" → spinner + "Criando sua conta…"
3. Após 1s → tela de sucesso com check verde e "Conta criada, Ana! 🎉"

### Passo 6 — Teste mobile (375px)

- Painel teal some
- Logo + Wordmark aparece no topo
- Grid two-col vira coluna única

### Passo 7 — Comparar com design

Abrir `design/Pense e Precifique/Cadastro.html` — visual deve ser idêntico.

---

## Checklist de validação

- [ ] Stepper com etapa 1 ativa (teal) e etapa 2 inativa
- [ ] Grid two-col: Nome+Empresa, Senha+Confirmar
- [ ] StrengthMeter aparece ao digitar senha, desaparece quando vazio
- [ ] 4 barras de força com cores corretas
- [ ] LogoUpload: estado vazio (tracejado) e com preview (circular)
- [ ] Validação de todos os campos
- [ ] E-mail em uso exibe erro específico
- [ ] Loading com spinner
- [ ] Estado de sucesso com check animado
- [ ] Mobile: coluna única, painel teal some
- [ ] Visual idêntico ao `Cadastro.html`
- [ ] `npm run dev` sem erros TypeScript

## Commit

```bash
git add .
git commit -m "feat(auth): adiciona CadastroPage com stepper, validação e upload de logo"
```

## Aprendizados desta implementação

*(preencher após executar)*

- ✅
- ⚠️
- 🔜

## Ao concluir

Informe **"C-015 concluído"** para:
1. Atualizar status no `CONVERSION_BACKLOG.md`
2. Mover este arquivo para `claude/prompts/concluidos/`
3. Receber o **C-016** (OnboardingPage)
