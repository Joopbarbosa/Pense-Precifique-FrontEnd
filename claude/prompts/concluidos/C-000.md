# C-000 — Setup do Projeto React

**Tipo:** inicialização  
**Depende de:** nada — este é o primeiro prompt  
**Executar em:** pasta `pense-precifique-frontend/` aberta no Claude Code  

---

## O que este prompt faz

Inicializa o projeto React completo com toda a estrutura de pastas definida
no `CLAUDE.md` do frontend. Ao concluir, o projeto deve rodar com `npm run dev`
sem erros em `http://localhost:5173`.

---

## Prompt

Cole este prompt no Claude Code:

---

Inicializa o projeto React para o **Pense & Precifique** na pasta atual (`pense-precifique-frontend/`).

### Stack
- Vite + React 18 + TypeScript
- Tailwind CSS v3
- React Router v6
- Zustand
- Axios

### Passo 1 — Inicializar o projeto Vite

```bash
npm create vite@latest . -- --template react-ts
```

Quando perguntar se pode sobrescrever a pasta, confirmar com `y`.

### Passo 2 — Instalar dependências

```bash
npm install
npm install tailwindcss@3 postcss autoprefixer
npm install react-router-dom
npm install zustand
npm install axios
npx tailwindcss init -p
```

### Passo 3 — Configurar `tailwind.config.ts`

Substituir o conteúdo pelo seguinte:

```ts
import type { Config } from 'tailwindcss'

export default {
  content: [
    './index.html',
    './src/**/*.{ts,tsx}',
  ],
  theme: {
    extend: {
      colors: {
        teal: {
          DEFAULT: '#2A9D8F',
          deep: '#1E7268',
        },
        orange: {
          DEFAULT: '#F97316',
        },
        dark: '#3A372F',
        line: '#EFEDE8',
      },
      borderRadius: {
        btn: '10px',
      },
      boxShadow: {
        card: '0 2px 8px rgba(0,0,0,0.06)',
      },
    },
  },
  plugins: [],
} satisfies Config
```

### Passo 4 — Criar `src/index.css`

Substituir todo o conteúdo pelo seguinte:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  --teal: #2A9D8F;
  --teal-deep: #1E7268;
  --orange: #F97316;
  --dark: #3A372F;
  --line: #EFEDE8;
  --r-btn: 10px;
  --shadow-card: 0 2px 8px rgba(0,0,0,0.06);
}

* {
  box-sizing: border-box;
}

body {
  background-color: #FAFAF8;
  color: #3A372F;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  margin: 0;
}
```

### Passo 5 — Criar estrutura de pastas completa em `src/`

Criar todas as pastas abaixo (vazias por enquanto):

```
src/
├── pages/
│   ├── auth/
│   ├── dashboard/
│   ├── clientes/
│   ├── orcamentos/
│   ├── insumos/
│   ├── produtos/
│   ├── producao/
│   └── configuracoes/
├── components/
│   ├── ui/
│   ├── layout/
│   └── shared/
├── hooks/
├── services/
├── store/
└── types/
```

### Passo 6 — Criar `src/types/index.ts`

```ts
// Tipos de domínio — Pense & Precifique

export type TipoProduto = 'PRODUTO' | 'PRODUTO_BASE' | 'CUSTOMIZACAO'

export type StatusOrcamento =
  | 'RASCUNHO'
  | 'ENVIADO'
  | 'APROVADO'
  | 'AGUARDANDO_SINAL'
  | 'SINAL_PAGO'
  | 'EM_PRODUCAO'
  | 'FINALIZADO'
  | 'ENTREGUE'
  | 'PAGO'
  | 'CANCELADO'

export type MotivoMovimentacao =
  | 'PRODUCAO'
  | 'ORCAMENTO'
  | 'PERDA'
  | 'AVARIA'
  | 'USO_EXTRA'
  | 'CORRECAO_ESTOQUE'
  | 'OUTRO'
```

### Passo 7 — Criar `src/services/api.ts`

```ts
import axios from 'axios'

const api = axios.create({
  baseURL: import.meta.env.VITE_API_URL ?? 'http://localhost:8080',
  headers: {
    'Content-Type': 'application/json',
  },
})

// Interceptor: injeta o token JWT em todas as requisições
api.interceptors.request.use((config) => {
  const token = localStorage.getItem('token')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

// Interceptor: redireciona para login se 401
api.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.response?.status === 401) {
      localStorage.removeItem('token')
      window.location.href = '/login'
    }
    return Promise.reject(error)
  }
)

export default api
```

### Passo 8 — Criar `src/store/authStore.ts`

```ts
import { create } from 'zustand'

interface Usuario {
  id: string
  nome: string
  email: string
}

interface AuthState {
  token: string | null
  usuario: Usuario | null
  setAuth: (token: string, usuario: Usuario) => void
  clearAuth: () => void
}

export const useAuthStore = create<AuthState>((set) => ({
  token: localStorage.getItem('token'),
  usuario: null,
  setAuth: (token, usuario) => {
    localStorage.setItem('token', token)
    set({ token, usuario })
  },
  clearAuth: () => {
    localStorage.removeItem('token')
    set({ token: null, usuario: null })
  },
}))
```

### Passo 9 — Criar `src/routes/index.tsx`

```tsx
import { createBrowserRouter } from 'react-router-dom'

// Placeholders — substituídos a cada C-XXX concluído
const Placeholder = ({ nome }: { nome: string }) => (
  <div style={{ padding: 32, fontFamily: 'sans-serif', color: '#3A372F' }}>
    <h2>{nome}</h2>
    <p style={{ color: '#888' }}>Tela em construção</p>
  </div>
)

export const router = createBrowserRouter([
  { path: '/',              element: <Placeholder nome="Login" /> },
  { path: '/login',         element: <Placeholder nome="Login" /> },
  { path: '/cadastro',      element: <Placeholder nome="Cadastro" /> },
  { path: '/onboarding',    element: <Placeholder nome="Onboarding" /> },
  { path: '/dashboard',     element: <Placeholder nome="Dashboard" /> },
  { path: '/clientes',      element: <Placeholder nome="Clientes" /> },
  { path: '/orcamentos',           element: <Placeholder nome="Lista de Orçamentos" /> },
  { path: '/orcamentos/novo',      element: <Placeholder nome="Criar Orçamento" /> },
  { path: '/orcamentos/:id',       element: <Placeholder nome="Detalhe do Orçamento" /> },
  { path: '/insumos',              element: <Placeholder nome="Lista de Insumos" /> },
  { path: '/insumos/novo',         element: <Placeholder nome="Cadastrar Insumo" /> },
  { path: '/insumos/:id',          element: <Placeholder nome="Detalhe do Insumo" /> },
  { path: '/produtos',             element: <Placeholder nome="Lista de Produtos" /> },
  { path: '/produtos/novo',        element: <Placeholder nome="Cadastrar Produto" /> },
  { path: '/produtos/:id/editar',  element: <Placeholder nome="Editar Produto" /> },
  { path: '/produtos/:id',         element: <Placeholder nome="Detalhe do Produto" /> },
  { path: '/producao',             element: <Placeholder nome="Registro de Produção" /> },
  { path: '/configuracoes',        element: <Placeholder nome="Configurações" /> },
])
```

### Passo 10 — Criar `src/App.tsx`

```tsx
import { RouterProvider } from 'react-router-dom'
import { router } from './routes'

export default function App() {
  return <RouterProvider router={router} />
}
```

### Passo 11 — Atualizar `src/main.tsx`

```tsx
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import App from './App'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>
)
```

### Passo 12 — Criar `.env.example`

```env
VITE_API_URL=http://localhost:8080
```

Criar também `.env` local com o mesmo conteúdo (não commitar).

### Passo 13 — Criar `Dockerfile`

```dockerfile
# Build
FROM node:20-alpine AS build
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

# Produção com Nginx
FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

### Passo 14 — Criar `nginx.conf`

```nginx
server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # SPA — redireciona todas as rotas para o index.html
  location / {
    try_files $uri $uri/ /index.html;
  }

  # Cache para assets estáticos
  location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
    expires 1y;
    add_header Cache-Control "public, immutable";
  }
}
```

### Passo 15 — Limpar arquivos padrão do Vite

Remover:
- `src/App.css`
- `src/assets/react.svg`
- `public/vite.svg`

---

**Resultado esperado:** `npm run dev` sobe sem erros em `http://localhost:5173` mostrando a página placeholder do Login.

---

## Checklist de validação

- [ ] `npm run dev` roda sem erros no terminal
- [ ] Browser abre `http://localhost:5173` e exibe "Login — Tela em construção"
- [ ] Navegar para `http://localhost:5173/dashboard` exibe "Dashboard — Tela em construção"
- [ ] Estrutura de pastas `src/pages/`, `src/components/`, `src/hooks/`, `src/services/`, `src/store/`, `src/types/` criadas
- [ ] `tailwind.config.ts` com as cores do projeto (`teal`, `orange`, `dark`, `line`)
- [ ] Variáveis CSS definidas em `src/index.css`
- [ ] `src/services/api.ts` criado com Axios + interceptores JWT
- [ ] `src/store/authStore.ts` criado com Zustand
- [ ] `src/types/index.ts` criado com `TipoProduto` e `StatusOrcamento`
- [ ] `Dockerfile` e `nginx.conf` criados

## Commit

```bash
git add .
git commit -m "chore(frontend): setup inicial React + Vite + TypeScript + Tailwind"
```

## Ao concluir

Informe **"C-000 concluído"** para:
1. Atualizar status no `CONVERSION_BACKLOG.md`
2. Mover este arquivo para `claude/prompts/concluidos/`
3. Receber o **C-001** (Logo + Wordmark)
