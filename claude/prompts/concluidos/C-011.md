# C-011 — SectionTitle

**Tipo:** componente shared  
**Depende de:** C-001 ✅  
**Usado em:** Cadastrar Insumo, Cadastrar Produto, Onboarding — formulários com seções numeradas  

---

## Prompt

Cole este prompt no Claude Code:

---

Cria o componente `SectionTitle` do Pense & Precifique.

**Localização:** `src/components/shared/SectionTitle.tsx`

**Visual de referência:** abrir `design/Pense e Precifique/Cadastrar-Insumo.html` — ver as seções "1 — Identificação", "2 — Medida e fracionamento", "3 — Estoque e custo".

---

### Props

```tsx
interface SectionTitleProps {
  number?: number | string    // número da seção (ex: 1, 2, "A")
  title: string               // título principal
  subtitle?: string           // descrição menor abaixo do título
  color?: string              // cor do número e do fundo (default: '#2A9D8F')
}
```

---

### Estrutura visual

```
┌────┐  Título da seção
│ 1  │  Subtítulo descritivo menor
└────┘
```

**Badge numérico:**
- `width: 28px`, `height: 28px`
- `borderRadius: 8px`
- `background: rgba(cor, 0.12)`
- `color: cor`
- `fontWeight: 700`, `fontSize: 13.5px`
- `display: grid`, `placeItems: center`
- `flexShrink: 0`

**Título:**
- `fontSize: 16px`, `fontWeight: 700`
- `color: #3A372F`
- `letterSpacing: -0.01em`
- `margin: 0`

**Subtítulo:**
- `fontSize: 12.5px`
- `color: #A29E96`
- `margin: 3px 0 0`

**Container:**
- `display: flex`, `alignItems: flex-start`, `gap: 13px`
- `marginBottom: 20px`

---

### Implementação

```tsx
import React from 'react'

interface SectionTitleProps {
  number?: number | string
  title: string
  subtitle?: string
  color?: string
}

export default function SectionTitle({
  number,
  title,
  subtitle,
  color = '#2A9D8F',
}: SectionTitleProps) {
  const bg = `rgba(${hexToRgb(color)}, 0.12)`

  return (
    <div style={{ display: 'flex', alignItems: 'flex-start', gap: 13, marginBottom: 20 }}>
      {number !== undefined && (
        <span style={{
          flexShrink: 0,
          width: 28,
          height: 28,
          borderRadius: 8,
          display: 'grid',
          placeItems: 'center',
          background: bg,
          color,
          fontWeight: 700,
          fontSize: 13.5,
        }}>
          {number}
        </span>
      )}
      <div>
        <h2 style={{
          margin: 0,
          fontSize: 16,
          fontWeight: 700,
          color: '#3A372F',
          letterSpacing: '-0.01em',
        }}>
          {title}
        </h2>
        {subtitle && (
          <p style={{ margin: '3px 0 0', fontSize: 12.5, color: '#A29E96' }}>
            {subtitle}
          </p>
        )}
      </div>
    </div>
  )
}

// Utilitário interno para converter hex em rgb
function hexToRgb(hex: string): string {
  const h = hex.replace('#', '')
  const n = parseInt(h.length === 3
    ? h.split('').map(c => c + c).join('')
    : h, 16)
  return `${(n >> 16) & 255}, ${(n >> 8) & 255}, ${n & 255}`
}
```

---

### Exportar

Criar `src/components/shared/index.ts`:
```ts
export { default as SectionTitle } from './SectionTitle'
```

---

### Página de teste temporária

Atualizar o `DashboardPreview` em `src/routes/index.tsx`:

```tsx
import { SectionTitle } from '../components/shared'

// Dentro do conteúdo do DashboardPreview:
<div style={{ padding: 24, maxWidth: 560 }}>
  <h2 style={{ color: '#3A372F', marginBottom: 32 }}>SectionTitle — Preview C-011</h2>

  {/* Com número e subtítulo — padrão dos formulários */}
  <SectionTitle
    number={1}
    title="Identificação"
    subtitle="Como você reconhece este insumo."
  />

  <SectionTitle
    number={2}
    title="Medida e fracionamento"
    subtitle="Como este insumo é medido e consumido."
  />

  <SectionTitle
    number={3}
    title="Estoque e custo"
    subtitle="O custo unitário é calculado automaticamente."
  />

  {/* Sem número */}
  <SectionTitle
    title="Ficha Técnica"
    subtitle="Componentes que formam este produto."
  />

  {/* Cor laranja (usado no onboarding) */}
  <SectionTitle
    number="→"
    title="Configuração inicial"
    subtitle="Defina seu valor/hora e margem padrão."
    color="#F97316"
  />
</div>
```

---

## Como testar

### Passo 1 — Subir o servidor
```bash
npm run dev
```
Abre `http://localhost:5174/dashboard`

---

### Passo 2 — O que você deve ver

```
SectionTitle — Preview C-011

[1]  Identificação
     Como você reconhece este insumo.

[2]  Medida e fracionamento
     Como este insumo é medido e consumido.

[3]  Estoque e custo
     O custo unitário é calculado automaticamente.

     Ficha Técnica
     Componentes que formam este produto.

[→]  Configuração inicial
     Defina seu valor/hora e margem padrão.
     (badge laranja)
```

### Passo 3 — Verificar cores

- Badge teal: fundo `rgba(42,157,143,0.12)`, número em `#2A9D8F`
- Badge laranja: fundo `rgba(249,115,22,0.12)`, símbolo em `#F97316`
- Título: `#3A372F`, bold
- Subtítulo: `#A29E96`, menor

### Passo 4 — Comparar com design

Abrir `design/Pense e Precifique/Cadastrar-Insumo.html` — as seções "1 Identificação", "2 Medida" e "3 Estoque" devem ser visualmente idênticas.

### Passo 5 — Se algo não funcionar

**Badge sem fundo colorido:**
Verificar se a função `hexToRgb` está correta e retornando os valores RGB separados por vírgula.

**Erro de TypeScript:**
```bash
npx tsc --noEmit
```

---

## Checklist de validação

- [ ] Badge numérico com fundo teal semi-transparente e número teal
- [ ] Título em 16px bold escuro
- [ ] Subtítulo em 12.5px cinza claro
- [ ] Sem número: só título e subtítulo alinhados
- [ ] Prop `color` altera badge para laranja corretamente
- [ ] Visual idêntico ao `Cadastrar-Insumo.html`
- [ ] `npm run dev` sem erros TypeScript

## Commit

```bash
git add .
git commit -m "feat(shared): adiciona componente SectionTitle com badge numerado"
```

## Aprendizados desta implementação

*(preencher após executar)*

- ✅
- ⚠️
- 🔜

## Ao concluir

Informe **"C-011 concluído"** para:
1. Atualizar status no `CONVERSION_BACKLOG.md`
2. Mover este arquivo para `claude/prompts/concluidos/`
3. Receber o **C-012** (ActionMenu)
