# C-016 — OnboardingPage

**Tipo:** tela  
**Depende de:** C-001 ✅, C-004 ✅ (Button), C-010 ✅ (Spinner)  
**Rota:** `/onboarding`  

---

## Prompt

Cole este prompt no Claude Code:

---

Cria a tela de Onboarding do Pense & Precifique — Etapa 2 de configuração inicial (valor/hora e margem de lucro).

**Localização:** `src/pages/auth/OnboardingPage.tsx`

**Visual de referência:** abrir `design/Pense e Precifique/Onboarding.html` no browser — reproduzir fielmente.

---

### Adicionar ícones em `src/components/ui/Icons.tsx`

```tsx
clock: (p?: React.SVGProps<SVGSVGElement>) => (
  <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" strokeWidth={1.7} strokeLinecap="round" strokeLinejoin="round" {...p}>
    <circle cx="12" cy="12" r="8.5"/>
    <path d="M12 7.5V12l3 1.8"/>
  </svg>
),
trendUp: (p?: React.SVGProps<SVGSVGElement>) => (
  <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" strokeWidth={1.7} strokeLinecap="round" strokeLinejoin="round" {...p}>
    <path d="M4 14.5 9 9l3.2 3L20 4.2"/>
    <path d="M15.5 4.2H20V8.7"/>
  </svg>
),
bulb: (p?: React.SVGProps<SVGSVGElement>) => (
  <svg viewBox="0 0 24 24" width="15" height="15" fill="none" stroke="currentColor" strokeWidth={1.7} strokeLinecap="round" strokeLinejoin="round" {...p}>
    <path d="M9 21h6M12 3a6 6 0 0 1 6 6c0 2.4-1.4 4.5-3 5.7V17H9v-2.3C7.4 13.5 6 11.4 6 9a6 6 0 0 1 6-6Z"/>
  </svg>
),
checkSmall: (p?: React.SVGProps<SVGSVGElement>) => (
  <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" strokeWidth={2.5} strokeLinecap="round" strokeLinejoin="round" {...p}>
    <path d="m5 12.5 4.2 4.2L19 7"/>
  </svg>
),
```

---

### Estrutura geral

Mesmo layout do Login e Cadastro — dois painéis, BrandPanel à esquerda, formulário à direita. Reutilizar `.login-card`, `.brand-col`, `.mobile-logo`.

Card: `width: min(1000px, 100%)`, `gridTemplateColumns: 1fr 1.15fr`

---

### Stepper — Etapa 2 ativa

```tsx
function Stepper() {
  return (
    <div style={{ display: 'flex', alignItems: 'center', gap: 14, marginBottom: 24 }}>

      {/* Etapa 1 — concluída */}
      <div style={{ display: 'flex', alignItems: 'center', gap: 9 }}>
        <span style={{
          width: 28, height: 28, borderRadius: '50%',
          display: 'grid', placeItems: 'center',
          background: 'rgba(42,157,143,0.14)', color: '#2A9D8F',
          border: '1.5px solid rgba(42,157,143,0.4)',
        }}>
          <Icons.checkSmall />
        </span>
        <span style={{ fontSize: 14, fontWeight: 500, color: '#A29E96', whiteSpace: 'nowrap' }}>
          Sua conta
        </span>
      </div>

      {/* Linha com gradiente */}
      <span style={{
        flex: 1, height: 2, minWidth: 24, borderRadius: 2,
        background: 'linear-gradient(90deg, rgba(42,157,143,0.5), #EFEDE8)',
      }} />

      {/* Etapa 2 — ativa */}
      <div style={{ display: 'flex', alignItems: 'center', gap: 9 }}>
        <span style={{
          width: 28, height: 28, borderRadius: '50%',
          display: 'grid', placeItems: 'center',
          fontSize: 13, fontWeight: 700,
          background: '#2A9D8F', color: '#fff',
          boxShadow: '0 4px 10px -4px rgba(42,157,143,0.7)',
        }}>2</span>
        <span style={{ fontSize: 14, fontWeight: 600, color: '#3A372F', whiteSpace: 'nowrap' }}>
          Precificação
        </span>
      </div>

    </div>
  )
}
```

---

### PriceField — campo de precificação com affix e dica

```tsx
interface PriceFieldProps {
  icon: React.ReactNode
  question: string
  explain: string
  affix: string
  affixSide: 'left' | 'right'
  placeholder: string
  dica: string
  value: string
  onChange: (v: string) => void
  inputMode?: 'decimal' | 'numeric'
}
```

Estrutura visual:
```
[ícone teal]  Pergunta principal
              Explicação em cinza

              ┌──────────────────────────────┐
              │ R$ │  25,00                  │  ← affix left
              └──────────────────────────────┘
              ou
              ┌──────────────────────────────┐
              │  40               │ %        │  ← affix right
              └──────────────────────────────┘

              ┌────────────────────────────────┐
              │ 💡  Dica explicativa em laranja │
              └────────────────────────────────┘
```

```tsx
function PriceField({ icon, question, explain, affix, affixSide, placeholder, dica, value, onChange, inputMode }: PriceFieldProps) {
  const [active, setActive] = useState(false)

  return (
    <div style={{ display: 'flex', gap: 14, alignItems: 'flex-start' }}>

      {/* Ícone */}
      <span style={{
        flexShrink: 0, marginTop: 2,
        width: 38, height: 38, borderRadius: 12,
        display: 'grid', placeItems: 'center',
        background: 'rgba(42,157,143,0.10)', color: '#2A9D8F',
      }}>{icon}</span>

      <div style={{ flex: 1, minWidth: 0 }}>
        {/* Label + descrição */}
        <label style={{ display: 'block', fontSize: 15.5, fontWeight: 600, color: '#3A372F', letterSpacing: '-0.01em' }}>
          {question}
        </label>
        <p style={{ margin: '4px 0 12px', fontSize: 13, color: '#A29E96', lineHeight: 1.5 }}>{explain}</p>

        {/* Input com affix */}
        <div style={{
          display: 'flex', alignItems: 'stretch', height: 54,
          borderRadius: 10, overflow: 'hidden',
          border: `1.5px solid ${active ? '#2A9D8F' : '#EFEDE8'}`,
          background: '#fff',
          boxShadow: active ? '0 0 0 4px rgba(42,157,143,0.12)' : 'none',
          transition: 'border-color .15s, box-shadow .15s',
        }}>
          {affixSide === 'left' && (
            <span style={{
              display: 'grid', placeItems: 'center',
              padding: '0 16px', fontSize: 16, fontWeight: 600,
              color: '#6B6860', background: '#FAF8F5',
              borderRight: '1px solid #EFEDE8',
            }}>{affix}</span>
          )}
          <input
            type="text"
            inputMode={inputMode}
            placeholder={placeholder}
            value={value}
            onChange={(e) => onChange(e.target.value.replace(/[^\d.,]/g, ''))}
            onFocus={() => setActive(true)}
            onBlur={() => setActive(false)}
            style={{
              flex: 1, minWidth: 0, border: 'none', outline: 'none',
              background: 'transparent', padding: '0 16px',
              fontSize: 18, fontWeight: 600, color: '#3A372F', fontFamily: 'inherit',
            }}
          />
          {affixSide === 'right' && (
            <span style={{
              display: 'grid', placeItems: 'center',
              padding: '0 18px', fontSize: 17, fontWeight: 600,
              color: '#6B6860', background: '#FAF8F5',
              borderLeft: '1px solid #EFEDE8',
            }}>{affix}</span>
          )}
        </div>

        {/* Dica laranja */}
        <div style={{
          display: 'flex', gap: 9, alignItems: 'flex-start', marginTop: 11,
          background: 'rgba(249,115,22,0.08)', border: '1px solid #FCE2CF',
          borderRadius: 12, padding: '11px 13px',
        }}>
          <span style={{ flexShrink: 0, color: '#EC7A2C', marginTop: 1 }}>
            <Icons.bulb />
          </span>
          <p style={{ margin: 0, fontSize: 12.7, lineHeight: 1.55, color: '#8A5A33' }}>{dica}</p>
        </div>
      </div>
    </div>
  )
}
```

---

### Formulário completo

```tsx
<form onSubmit={handleSubmit} style={{ display: 'flex', flexDirection: 'column', gap: 24 }}>

  <PriceField
    icon={<Icons.clock />}
    question="Quanto vale a sua hora de trabalho?"
    explain="Este valor entra no cálculo de custo de mão de obra de cada produto."
    affix="R$" affixSide="left"
    placeholder="25,00"
    inputMode="decimal"
    dica="Exemplo: se você leva 2h para fazer um produto e sua hora vale R$ 25, o custo de mão de obra é R$ 50."
    value={hora}
    onChange={setHora}
  />

  <PriceField
    icon={<Icons.trendUp />}
    question="Qual é a sua margem de lucro padrão?"
    explain="Percentual adicionado ao custo total para formar seu preço de venda."
    affix="%" affixSide="right"
    placeholder="40"
    inputMode="numeric"
    dica="Você poderá ajustar a margem produto a produto quando necessário."
    value={margem}
    onChange={setMargem}
  />

  <Button
    variant="primary"
    type="submit"
    fullWidth
    disabled={loading}
    size="lg"
    icon={loading ? <Spinner size={16} /> : undefined}
    iconRight={loading ? undefined : <span style={{ fontSize: 19, marginTop: -1 }}>→</span>}
  >
    {loading ? 'Preparando tudo…' : 'Começar a usar o sistema'}
  </Button>

</form>

{/* Nota de rodapé */}
<p style={{
  margin: '18px 0 0', display: 'flex', flexWrap: 'wrap',
  alignItems: 'center', justifyContent: 'center', gap: 6,
  fontSize: 12.7, color: '#A29E96', textAlign: 'center',
}}>
  <span style={{ color: '#2A9D8F', display: 'flex' }}><Icons.gear /></span>
  <span>Dá pra mudar tudo depois em <strong style={{ fontWeight: 600, color: '#6B6860' }}>Configurações</strong>.</span>
</p>
```

---

### Estado e lógica

```tsx
const [hora, setHora] = useState('')
const [margem, setMargem] = useState('')
const [loading, setLoading] = useState(false)

function handleSubmit(e: React.FormEvent) {
  e.preventDefault()
  setLoading(true)
  // Mock: simula salvar configurações
  setTimeout(() => {
    setLoading(false)
    // Em produção: navigate('/dashboard')
  }, 1100)
}
```

---

### Atualizar rota em `src/routes/index.tsx`

```tsx
import OnboardingPage from '../pages/auth/OnboardingPage'

{ path: '/onboarding', element: <OnboardingPage /> },
```

---

## Como testar

### Passo 1 — Subir o servidor
```bash
npm run dev
```
Abre `http://localhost:5174/onboarding`

---

### Passo 2 — O que você deve ver

```
┌────────────────┬───────────────────────────────────────────┐
│  BrandPanel    │  [✓ Sua conta] ──── [2 Precificação]      │
│  teal          │                                           │
│                │  Configure como você quer precificar 💡   │
│                │  Você pode alterar nas Configurações.     │
│                │                                           │
│                │  [⏱]  Quanto vale a sua hora?             │
│                │       Este valor entra no cálculo...      │
│                │  ┌─────────────────────────┐              │
│                │  │ R$ │  25,00             │              │
│                │  └─────────────────────────┘              │
│                │  [ 💡 Exemplo: se você leva 2h... ]       │
│                │                                           │
│                │  [📈]  Qual é sua margem padrão?          │
│                │  ┌─────────────────────────┐              │
│                │  │ 40          │ %          │              │
│                │  └─────────────────────────┘              │
│                │  [ 💡 Você poderá ajustar... ]            │
│                │                                           │
│                │  [ Começar a usar o sistema → ]           │
│                │  ⚙ Dá pra mudar depois em Configurações  │
└────────────────┴───────────────────────────────────────────┘
```

### Passo 3 — Verificar Stepper

- Etapa 1 "Sua conta": círculo teal com check ✓, texto cinza
- Linha com gradiente teal → cinza
- Etapa 2 "Precificação": círculo teal sólido com "2", texto escuro bold

### Passo 4 — Testar campos

1. Clicar no campo R$ → borda teal + sombra teal
2. Digitar "25,00" → aceita apenas números, vírgula e ponto
3. Clicar no campo % → mesmo comportamento
4. Tentar digitar letras → ignoradas

### Passo 5 — Testar submit

1. Clicar "Começar a usar o sistema"
2. Spinner aparece + "Preparando tudo…"
3. Após 1,1s volta ao estado normal

### Passo 6 — Teste mobile (375px)

- Painel teal some
- Logo aparece no topo
- PriceFields em coluna única

### Passo 7 — Comparar com design

Abrir `design/Pense e Precifique/Onboarding.html` — visual deve ser idêntico.

---

## Checklist de validação

- [ ] Stepper: etapa 1 com check teal, etapa 2 ativa com "2" teal
- [ ] Linha do stepper com gradiente teal → cinza
- [ ] PriceField valor/hora: affix "R$" à esquerda
- [ ] PriceField margem: affix "%" à direita
- [ ] Foco no campo: borda teal + sombra teal
- [ ] Dicas laranja com ícone bulb e fundo `rgba(249,115,22,0.08)`
- [ ] Filtro de input: só aceita números, vírgula e ponto
- [ ] Botão "Começar" com loading spinner
- [ ] Nota de rodapé com ícone gear teal
- [ ] Mobile: painel teal some, coluna única
- [ ] Visual idêntico ao `Onboarding.html`
- [ ] `npm run dev` sem erros TypeScript

## Commit

```bash
git add .
git commit -m "feat(auth): adiciona OnboardingPage com PriceField e stepper etapa 2"
```

## Aprendizados desta implementação

*(preencher após executar)*

- ✅
- ⚠️
- 🔜

## Ao concluir

Informe **"C-016 concluído"** para:
1. Atualizar status no `CONVERSION_BACKLOG.md`
2. Mover este arquivo para `claude/prompts/concluidos/`
3. Receber o **C-017** (DashboardPage)
